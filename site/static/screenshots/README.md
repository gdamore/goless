Add screenshot assets for the docs site here.

Store files in `site/static/screenshots/` and reference them from docs with
absolute public paths such as `/screenshots/example.png` or
`![Initial view](/screenshots/initial-view.png)`.

Avoid relative image links in Markdown so pages keep working after Hugo moves
content to its final output paths.

Suggested usage:

- user-manual walkthrough screenshots
- chrome or status bar callouts
- before/after examples for embedder recipes
